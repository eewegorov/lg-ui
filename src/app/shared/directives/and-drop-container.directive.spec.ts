import { AndDropContainerDirective } from './and-drop-container.directive';

describe('AndDropContainerDirective', () => {
  it('should create an instance', () => {
    const directive = new AndDropContainerDirective();
    expect(directive).toBeTruthy();
  });
});
